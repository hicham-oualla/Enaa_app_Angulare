export class JustificationRetard {
  id!: number;
  name!: string;      // The name of the file (PDF)
  type!: string;      // The type of file (e.g., application/pdf)
  pdfData!: string;   // The PDF data (as base64 encoded string)


  retardId!: number;


}
